import React from 'react';
import { Center, Title } from '@nx-fullstack/ui-components';

const App: React.FC = () => {
  return (
    <Center>
      <Title>Web</Title>
    </Center>
  );
};

export default App;
